//To create program like this. First import a MP3 file. Next copy <audio src=".mp3" controls></audio>. After that get the name of the file "without the .mp3 part" and put it inbetween the first " and the first . Then say the code as a .html
